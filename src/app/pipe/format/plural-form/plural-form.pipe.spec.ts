import { PluralFormPipe } from './plural-form.pipe';

describe('PluralFormPipe', () => {
  it('create an instance', () => {
    const pipe = new PluralFormPipe();
    expect(pipe).toBeTruthy();
  });
});
